node_modules/

# Контент статей (HTML + картинки) — вне git: репозиторий хранит только
# приложение и стек деплоя. Полный архив разворачивается на сервере отдельно.
# (data/articles/index.json и data/*.json — индексы приложения, остаются.)
data/articles/*/

# временные файлы проверки/сборки
.verify/
_*.mjs
_extracted/

# локальные настройки Claude Code
.claude/settings.local.json

# системное
.DS_Store
Thumbs.db
*.log
